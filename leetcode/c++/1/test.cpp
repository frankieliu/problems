#include <gtest/gtest.h>
#include "../base/cpp_standards.h"
#include "unordered-map.hpp"

/*
*/

TEST(Leetcode, unordered_map_1) {
    Solution s;
    vector<int> a={};
    // EXPECT_EQ(, s.(a));
    vector<int> b={};
    // EXPECT_EQ(, s.(b));
    s.printme();
}
