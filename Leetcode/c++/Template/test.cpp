#include <gtest/gtest.h>
#include "../base/cpp_standards.h"
#include ".hpp"

/*
*/

TEST(Leetcode, _) {
    Solution s;
    vector<int> a={,};
    EXPECT_EQ(, s.(a));
    vector<int> b={,};
    EXPECT_EQ(, s.(b));
}
