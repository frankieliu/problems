#include <gtest/gtest.h>
#include "../base/cpp_standards.h"
#include "%title%.hpp"

/*
*/

TEST(Leetcode, %title_%) {
    Solution s;
    vector<int> a={,};
    EXPECT_EQ(, s.(a));
    vector<int> b={,};
    EXPECT_EQ(, s.(b));
}
