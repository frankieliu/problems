#include <bits/stdc++.h>

using namespace std;

void miniMaxSum(vector <long> out, vector <long> in) {
  
  miniMaxSum(out,in)
}

void miniMaxSum(vector <int> arr) {
    // Complete this function
}

int main() {
    vector<int> arr(5);
    for(int arr_i = 0; arr_i < 5; arr_i++){
       cin >> arr[arr_i];
    }
    miniMaxSum(arr);
    return 0;
}
